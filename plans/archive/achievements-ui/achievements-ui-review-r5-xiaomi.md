All four R4 codex concerns are addressed:

- **CSS `*` removal**: `--top-action-step` precomputed; `+ step + step` used instead of `* 2`. ✅
- **Unified unlock rule**: `compute || sdk || persisted` for all types; tests cover cross-device persisted case. ✅
- **`iconKey` dropped**: type no longer has it; parity test + `achievementIconsExist` test cover all derivations from `tag`. ✅
- **TitleScene button order**: both "with community" and "without community" sections consistently gate on `sdk.canUseAchievements() && prologueShown`. ✅

No new MAJOR or CRITICAL gaps introduced in R5. The plan is implementation-ready.

CONCERNS REMAIN — but only because the R4 verdict line said "CONCERNS REMAIN" and I'm reviewing the fix. In fact:

NO SIGNIFICANT CONCERNS