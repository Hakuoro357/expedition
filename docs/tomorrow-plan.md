# План на следующий рабочий день

После v0.3.42 (2026-05-02). Главный gating item — наблюдение за квотой
gp.player.sync в дашборде GamePush.

## Приоритет 1 — релиз v0.3.42 и наблюдение

1. **Залить `builds/gamepush/solitaire-expedition-v0.3.42.zip` в GamePush.**
   Acceptance: версия активна в дашборде, smoke-test playthrough в sandbox
   проходит без ошибок в console.
2. **Наблюдать счётчик API-вызовов в течение суток.** Базовая линия — 79k
   на конец 2026-05-02. Цель — увидеть сокращение до 15–20k. Acceptance:
   через 24 часа после релиза счётчик в дашборде показывает падение ≥
   60% от вчерашнего пика.
3. **Подтвердить, что es/pt/de/fr cloud-save больше не сбрасывается.**
   Acceptance: создать тестовый аккаунт с локалью de или fr через `?lang=`,
   сыграть до точки c1n2, перезагрузить вкладку — прогресс на месте.
4. **Подтвердить, что daily-партия восстанавливается из cloud.**
   Acceptance: открыть daily, сделать 3-5 ходов, закрыть вкладку, открыть
   снова — currentGame подтянулся, не сброшен.

## Приоритет 2 — UX-фидбек по сегодняшним фиксам

5. **Шрифт карт Georgia 14px — собрать фидбек.** Если в течение дня от
   пользователей не поступит жалоб типа «теперь буквы слишком крупные»
   или «не нравится засечный шрифт» — фикс приживается. Иначе откатить.
6. **Android-фикс описания карты — подтвердить в OK app.** Acceptance:
   открыть игру в Одноклассниках с Android-устройства, проверить что
   третья строка описания самой длинной точки видна целиком.

## Приоритет 3 — техдолг и доработки

7. **Если сократить квоту не удалось до 50k и ниже** — разобрать почему.
   Подозрения: gp.gameplayStart/Stop частые, gp.ads.* в showSticky/
   refreshSticky могут считаться отдельно. План B: ещё агрессивнее
   debounce, или skip sync на mute/volume changes.
8. **Документация QWEN.md / TASKS.md / progress.md устарели**,
   PUBLISH_YANDEX.md де-факто source of truth. **Не трогать сейчас**
   — admin-чурн, низкий ROI пока релизы идут потоком.

## НЕ делать

- GameScene decomposition (план записан в `docs/specs/2026-05-02-gamescene-
  decomposition.md`). Триггеры: line count > 2000 или фича требует 3+
  кластеров в одном PR. Сейчас ни один не сработал.
- Не добавлять новые языки или локали — фокус на стабилизации существующих.

## Известные блокеры

- Жду подтверждения от пользователя что v0.3.42 действительно сократил
  расход квоты (ждать 24 часа после релиза).
- Если квота продолжит расти теми же темпами — это сигнал, что узкое
  горлышко не в `setCloudSave`, а в каком-то другом SDK-вызове, и план
  нужен новый.

## Артефакты сегодняшнего дня (для контекста)

- 7 коммитов на `feature/gamepush-migration`, push сделан, upstream
  настроен (`origin/feature/gamepush-migration`).
- v0.3.42 собран и упакован: `builds/gamepush/solitaire-expedition-v0.3.42.zip`.
- 119 тестов проходят (было 101, +18 за сегодня).
- План декомпозиции GameScene прошёл qwen-loop за 3 раунда (16
  замечаний, все приняты, консенсус). Финал в
  `docs/specs/2026-05-02-gamescene-decomposition.md`.
- Знание про квоту GamePush записано в `~/.claude/CLAUDE.md` и
  `~/.claude/plan-mistakes/gamepush.md` для всех будущих игровых проектов.
